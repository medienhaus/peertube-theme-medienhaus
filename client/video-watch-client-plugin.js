function register ({ registerHook, peertubeHelpers }) {
  
  /*
  document.querySelectorAll(".menu-wrapper").forEach(el => el.remove());
  
  var mainContent = document.querySelector("#content")
  mainContent.classList.add("expanded");
  
  

    var isPresentingVideo = document.querySelector("#video-wrapper");
    console.log(isPresentingVideo)
    if(isPresentingVideo){
      console.log('Videoplayer')
    }

  */
  
  /*
  var isPresentingVideo = $('#video-wrapper');
  
  if(isPresentingVideo){
    $('#content').addClass('expanded');
    $('.menu-wrapper').remove();
  }
  */
  
}

export {
  register
}
